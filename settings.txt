You will need to fill out this file for hand in. 

The bellow is just an example, feel free to disregard this documents contents.


Your final settings.txt will look something like this when you are finished:

/////////////////////////////////

Wallpaper 1:

//your parameter variables go here!
let outRectLoc = 25                 //Location of "microchip"
let outRectWH = 50                  //Width of "microchip"
let mirror = true                   //Mirror the "microchip" when outRectLoc + outRectWH is less than 100
let connectAmount = 5               //Amount of "wires" connected to "microchip" 
let connecterOrientation = 1        //Orientation of "wires" (1 = x axis), (2 = y axis), (3 = both)                     
let circleSize = 5                  //Size of circles

let randomCirclePos = false         //If this is false then you can manually set the positions of the circles in circlePos array, if true it will randomise the positions.
let pairs = 10                      //Amount of pairs of circles, if randomCirclePos = true, it will use this number, if randomCirclePos = false, it will automatically choose the number based on circlePos array
var circlePos = [130,115,30,85,150,115,150,100,150,100,50,85,170,115,170,85,75,85,170,85,70,15,170,-15,50,15,50,0,50,0,150,-15,30,15,30,-15,30,-15,130,-15]
//^^ If randomCirclePos = false, you can input the positions of the circles manually, if randomCirclePos = true, this array will be filled based on pairs variable

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_PORTRAIT);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 0;
}
function wallpaper_background() {
  background(33, 82, 194); // blue
}
/////////////////////////////////

Wallpaper 2:

//your parameter variables go here!
let outRectLoc = 60                 //Location of "microchip"
let outRectWH = 75                 //Width of "microchip"
let mirror = true                   //Mirror the "microchip" when outRectLoc + outRectWH is less than 100
let connectAmount = 3               //Amount of "wires" connected to "microchip" 
let connecterOrientation = 3        //Orientation of "wires" (1 = x axis), (2 = y axis), (3 = both)                     
let circleSize = 5                 //Size of circles

let randomCirclePos = false         //If this is false then you can manually set the positions of the circles in circlePos array, if true it will randomise the positions.
let pairs = 10                      //Amount of pairs of circles, if randomCirclePos = true, it will use this number, if randomCirclePos = false, it will automatically choose the number based on circlePos array

var circlePos = [65,50,50,65,50,65,-50,65,-65,50,-50,65,65,50,65,-50,65,-50,50,-65,65,-50,50,-65,50,-65,-50,-65,-65,-50,-50,-65,-65,-50,-65,50]
//^^ If randomCirclePos = false, you can input the positions of the circles manually, if randomCirclePos = true, this array will be filled based on pairs variable

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_PORTRAIT);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 0;
}
function wallpaper_background() {
  background(33, 82, 194); // blue
}
////////////////////////////////

Wallpaper 3:

//your parameter variables go here!
let outRectLoc = 60                 //Location of "microchip"
let outRectWH = 75                 //Width of "microchip"
let mirror = true                   //Mirror the "microchip" when outRectLoc + outRectWH is less than 100
let connectAmount = 6               //Amount of "wires" connected to "microchip" 
let connecterOrientation = 2        //Orientation of "wires" (1 = x axis), (2 = y axis), (3 = both)                     
let circleSize = 10                 //Size of circles

let randomCirclePos = false         //If this is false then you can manually set the positions of the circles in circlePos array, if true it will randomise the positions.
let pairs = 10                      //Amount of pairs of circles, if randomCirclePos = true, it will use this number, if randomCirclePos = false, it will automatically choose the number based on circlePos array

var circlePos = [50,70,-50,70,50,90,-50,90,50,110,-50,110,-50,110,-50,270,50,70,50,110,165,40,165,-60,185,20,185,-40]
//^^ If randomCirclePos = false, you can input the positions of the circles manually, if randomCirclePos = true, this array will be filled based on pairs variable


function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_PORTRAIT);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 0;
}
function wallpaper_background() {
  background(33, 82, 194); // blue
}

////////////////////////////////

Wallpaper 4:

//your parameter variables go here!
let outRectLoc = 0                 //Location of "microchip"
let outRectWH = 45                 //Width of "microchip"
let mirror = true                   //Mirror the "microchip" when outRectLoc + outRectWH is less than 100
let connectAmount = 4               //Amount of "wires" connected to "microchip" 
let connecterOrientation = 2        //Orientation of "wires" (1 = x axis), (2 = y axis), (3 = both)                     
let circleSize = 7                 //Size of circles

let randomCirclePos = false         //If this is false then you can manually set the positions of the circles in circlePos array, if true it will randomise the positions.
let pairs = 10                      //Amount of pairs of circles, if randomCirclePos = true, it will use this number, if randomCirclePos = false, it will automatically choose the number based on circlePos array

var circlePos = [75,75,125,125, 125,125,75,75, 140,-20,75,75, 60,223,125,125, 140,50,140,50, 50,155,50,155]
//^^ If randomCirclePos = false, you can input the positions of the circles manually, if randomCirclePos = true, this array will be filled based on pairs variable


function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_PORTRAIT);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 0;
}
function wallpaper_background() {
  background(33, 82, 194); // blue
}

////////////////////////////////

Wallpaper 5:

//your parameter variables go here!
let outRectLoc = 13.5               //Location of "microchip"
let outRectWH = 75                  //Width of "microchip"
let mirror = true                   //Mirror the "microchip" when outRectLoc + outRectWH is less than 100
let connectAmount = 2               //Amount of "wires" connected to "microchip" 
let connecterOrientation = 2        //Orientation of "wires" (1 = x axis), (2 = y axis), (3 = both)                     
let circleSize = 9                  //Size of circles

let randomCirclePos = false         //If this is false then you can manually set the positions of the circles in circlePos array, if true it will randomise the positions.
let pairs = 10                      //Amount of pairs of circles, if randomCirclePos = true, it will use this number, if randomCirclePos = false, it will automatically choose the number based on circlePos array

var circlePos = [70,0,125,0, 70,100,125,0, 70,100,70,200, 110,20,110,80, 90,120,90,180, 25,120,25,120, 25,170,25,170, 175,30,175,30, 175,80,175,80]
//^^ If randomCirclePos = false, you can input the positions of the circles manually, if randomCirclePos = true, this array will be filled based on pairs variable


function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_PORTRAIT);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 0;
}
function wallpaper_background() {
  background(33, 82, 194); // blue
}
////////////////////////////////

Wallpaper 6:

//your parameter variables go here!
let outRectLoc = 0               //Location of "microchip"
let outRectWH = 25                  //Width of "microchip"
let mirror = true                   //Mirror the "microchip" when outRectLoc + outRectWH is less than 100
let connectAmount = 3               //Amount of "wires" connected to "microchip" 
let connecterOrientation = 3        //Orientation of "wires" (1 = x axis), (2 = y axis), (3 = both)                     
let circleSize = 10                  //Size of circles

let randomCirclePos = false         //If this is false then you can manually set the positions of the circles in circlePos array, if true it will randomise the positions.
let pairs = 10                      //Amount of pairs of circles, if randomCirclePos = true, it will use this number, if randomCirclePos = false, it will automatically choose the number based on circlePos array

var circlePos = [35,30,35,170, 50,30,50,170, 27,90,170,100, 27,110,170,120, 27,130,170,140, 100,170,100,-30, 115,170,115,-30, 170,30,130,75, 153,55]
//^^ If randomCirclePos = false, you can input the positions of the circles manually, if randomCirclePos = true, this array will be filled based on pairs variable


function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_PORTRAIT);
  pWallpaper.show_guide(true); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 0;
}
function wallpaper_background() {
  background(33, 82, 194); // blue
}
////////////////////////////////

Wallpaper 7:

//your parameter variables go here!
let outRectLoc = 100               //Location of "microchip"
let outRectWH = 50                  //Width of "microchip"
let mirror = true                   //Mirror the "microchip" when outRectLoc + outRectWH is less than 100
let connectAmount = 5               //Amount of "wires" connected to "microchip" 
let connecterOrientation = 0        //Orientation of "wires" (1 = x axis), (2 = y axis), (3 = both)                     
let circleSize = 6                  //Size of circles

let randomCirclePos = false         //If this is false then you can manually set the positions of the circles in circlePos array, if true it will randomise the positions.
let pairs = 10                      //Amount of pairs of circles, if randomCirclePos = true, it will use this number, if randomCirclePos = false, it will automatically choose the number based on circlePos array

var circlePos = [150,50,125,90, 200,10,150,50, 190,190,125,160, 125,160,60,130, 125,90,60,130, 0,0,0,200, 20,0,20,200, 200,25,200,175, 180,25,180,175, 150,75,150,75, 150,170,160,170]
//^^ If randomCirclePos = false, you can input the positions of the circles manually, if randomCirclePos = true, this array will be filled based on pairs variable


function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GLIDE_WALLPAPER);
  pWallpaper.resolution(NINE_PORTRAIT);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 0;
}
function wallpaper_background() {
  background(33, 82, 194); // blue
}

////////////////////////////////

Wallpaper 8:
//your parameter variables go here!
let outRectLoc = 50               //Location of "microchip"
let outRectWH = 25                  //Width of "microchip"
let mirror = true                   //Mirror the "microchip" when outRectLoc + outRectWH is less than 100
let connectAmount = 3               //Amount of "wires" connected to "microchip" 
let connecterOrientation = 3        //Orientation of "wires" (1 = x axis), (2 = y axis), (3 = both)                     
let circleSize = 5                 //Size of circles

let randomCirclePos = false         //If this is false then you can manually set the positions of the circles in circlePos array, if true it will randomise the positions.
let pairs = 10                      //Amount of pairs of circles, if randomCirclePos = true, it will use this number, if randomCirclePos = false, it will automatically choose the number based on circlePos array

var circlePos = [80,80,80,80, 90,80,90,100, 100,80,100,120, 110,80,110,80, 120,100,120,100, 110,120,120,110, 40,80,-40,80, 40,95,-40,120, 40,120,-20,105, -20,105,40,105, 100,40,100,40, 100,0,100,0, 100,-40,100,-40, 200,160,200,240, 160,200,240,200, 180,180,220,180, 220,180,220,220, 220,220,180,220, 180,220,180,180]
//^^ If randomCirclePos = false, you can input the positions of the circles manually, if randomCirclePos = true, this array will be filled based on pairs variable


function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_PORTRAIT);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 0;
}
function wallpaper_background() {
  background(33, 82, 194); //blue
}

////////////////////////////////

Wallpaper 9:
//your parameter variables go here!
let outRectLoc = 60               //Location of "microchip"
let outRectWH = 80                  //Width of "microchip"
let mirror = false                   //Mirror the "microchip" when outRectLoc + outRectWH is less than 100
let connectAmount = 0               //Amount of "wires" connected to "microchip" 
let connecterOrientation = 2        //Orientation of "wires" (1 = x axis), (2 = y axis), (3 = both)                     
let circleSize = 20                 //Size of circles

let randomCirclePos = false         //If this is false then you can manually set the positions of the circles in circlePos array, if true it will randomise the positions.
let pairs = 10                      //Amount of pairs of circles, if randomCirclePos = true, it will use this number, if randomCirclePos = false, it will automatically choose the number based on circlePos array

var circlePos = [25,25,175,175, 175,175,25,25, 0,100,0,-70, 47,47,47,47, 150,150,150,150]
//^^ If randomCirclePos = false, you can input the positions of the circles manually, if randomCirclePos = true, this array will be filled based on pairs variable


function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_PORTRAIT);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 0;
}
function wallpaper_background() {
  background(33, 82, 194); //blue
}
